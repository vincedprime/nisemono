# Nisemono

A dependency-free static website.

## Local preview

Open `index.html` in a browser, or run any static file server from the project root.

## Deployment

Pushing to `main` or `codex/static-github-pages` deploys the repository root to GitHub Pages through GitHub Actions. In the repository settings, select **GitHub Actions** as the Pages source if it is not already selected.
